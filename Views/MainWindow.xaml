<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:NullEngine.ViewModels;assembly=NullEngine"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" WindowState="Maximized" Width="800" Height="600"
        x:Class="NullEngine.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="NullEngine" >

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

  <Grid ColumnDefinitions="*" RowDefinitions="*">
    <Image Name="Frame" Stretch="UniformToFill" MinWidth="100" MinHeight="100"/>
    <TextBlock Name="Info" Foreground="White" Background="Black" HorizontalAlignment="Left" VerticalAlignment="Top"/>
  </Grid>
</Window>
